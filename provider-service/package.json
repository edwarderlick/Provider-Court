{
  "name": "provider-service",
  "version": "0.1.0",
  "private": true,
  "description": "Off-chain Provider #1 generation service for Provider Court: generates text/image/audio for accepted jobs and pins the result to IPFS for the Intelligent Contract's adjudicate(job_id, cid) to fetch independently.",
  "scripts": {
    "dev": "vercel dev",
    "test:harness": "tsx test/test-harness.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^5.1.14",
    "typescript": "^5.7.2",
    "tsx": "^4.19.2",
    "@types/node": "^22.10.2",
    "dotenv": "^16.4.7"
  }
}
